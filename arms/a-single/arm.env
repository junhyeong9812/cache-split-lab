# ── arm A — a-single ─────────────────────────────────────────────────────
# 1 노드 × 캐시 N.  자(ruler) — 캐시를 쪼개지 않았을 때의 천장.
# 이 arm 이 없으면 B/C 의 히트율이 좋은 건지 나쁜 건지 말할 수 없다.
ARM_ID=a-single
ARM_UPSTREAM_CONF=arm-a.conf

# profile 없음 → app-2 를 띄우지 않는다 (노드 1개)
COMPOSE_PROFILES=
