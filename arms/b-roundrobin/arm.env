# ── arm B — b-roundrobin ─────────────────────────────────────────────────
# 2 노드 × 캐시 N/2, 라운드로빈.  함정.
# 두 노드가 각자 전체 키 공간을 보고 같은 키를 중복 캐싱한다 → 유효 캐시 = N/2
ARM_ID=b-roundrobin
ARM_UPSTREAM_CONF=arm-b.conf

COMPOSE_PROFILES=two-node
