# ── arm C — c-keyhash ────────────────────────────────────────────────────
# 2 노드 × 캐시 N/2, 키 해시.  해소 — 그리고 그 대가(핫키 쏠림).
# 각 노드가 자기 몫 키만 본다 → 중복 없음 → 유효 캐시 = N
ARM_ID=c-keyhash
ARM_UPSTREAM_CONF=arm-c.conf

COMPOSE_PROFILES=two-node
