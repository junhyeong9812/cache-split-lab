# TODO — 세션 이어서 하기

> 2026-07-19 갱신. **프로젝트 1차 완결** — Phase 1(s=0)·0(포화)·4(부하 지도)·D(외부화)
> 실측 종료, 핵심 질문 전부 닫힘. 아래 "다음 작업 후보"는 선택 항목(백로그)이다.

---

## 세션 ID (이어서 재개용)

```
7eef03de-5330-44d8-87b6-3cc2275dc30d
```

- 새 세션이면 이 파일부터 읽으면 된다 — 리그 상태는 깨끗(BPC/APC 컨테이너 정리됨).

## 지금 당장 (재개 첫 액션)

**없음 — 리그·리포 모두 깨끗한 상태로 종결.** 바로 아래 "다음 작업"으로 진입하면 된다.
(주의 하나: `docs/measurement-log.md` 의 "머지 후 결함" 열이 `이월(대기)` — 재개 시
푸시된 커밋들에 문제가 없었는지 1문으로 확인해 소급 기입.)

## 완료된 것 (2026-07-19 세션)

- ✅ **부하 생성기 결함의 진짜 원인** — f8d037f 는 불완전(원소 1개 래핑 = VU 마다 CDF
  사본). 원소 40k 개 SharedArray 로 수정, 통제실험·verify-zipf 동등성 검정으로 확인.
  probe.js VU 과할당(15,000)도 축소. `docs/TIMELINE.md` 문제 ⑥.
- ✅ **Phase 0** — 무릎 A=5000 / B=4500 / C=4000 (CPU 총량 0.5 로 조임, APC 천장 10.7k).
  C 무릎 = 노드 용량 ÷ 핫 노드 몫(0.628), 오차 0.5%. `docs/phases/phase-0-saturation/`.
- ✅ **Phase 4** — 80~120% 지도. **역전 실측**: s=1.5 에서 히트율 축 평평, C 가 p50
  42~104배로 먼저 붕괴. "라운드로빈이 항상 진다" 반증(README 미해결 ③ 해소).
  `docs/phases/phase-4-load/`.
- ✅ 듀얼 리뷰(Claude 워커 ∥ codex) — `docs/plans/2026-07-18/phase4-saturation-crosspoint/review-log.md`.

## ✅ 3번 완료 — 레디스 외부화 (arm D, 2026-07-19)

구현(KeyCache 인터페이스·RedisLruCache — Lua ZSET+HASH 엔트리 정확 LRU·compose redis
profile·run_arm.py d-redis 등록) + Lua 축출 순서 검증 + 사전 등록(P1~P4, 3개 일치) 실측
완료. **무릎 3000(A 의 60%) · 저부하 세금 +0.08ms 뿐 · 히트율 ≡ A(연역 확인) · 과부하
goodput 역행** — "외부화의 대가는 지연이 아니라 용량". `docs/phases/phase-d-redis/` 3종.

## 다음 작업 후보 (선택 — 백로그)

1. ~~Phase D goodput 역행 규명~~ → **해소**(2026-07-19 판별 런: 아티팩트 아닌 실체·
   진행성 — `phase-d-redis/gate.md` 판별 런 절). 잔여 정밀도: ~5s 절단 계층 특정.
2. **REDIS_FRAC 감도** (0.1/0.3 무릎 이동 — 외부화 최적 CPU 배분 곡선) — 선택.
3. **Phase 2 (s=1) 스윕** — 히트율 지도 중간 칸 + phase-0 P2 확정 반복 병행 — 선택.
4. **arm E — 마스터/레플리카**: **기각 권고로 종결**(결론이 연역되고 구현 비용 최대 —
   정량화가 필요해지면 재론). 재론 시 별도 정의 게이트.

## 남은 페이즈 (마스터: `docs/EXPERIMENTS.md`)

| 페이즈 | 상태 |
|--------|------|
| Phase 2 (s=1) | 예비 1점만. 히트율 스윕 + (권장) 부하 그리드 1회 — 쏠림 ~0.55 예상 |
| Phase 3 (s=2) | 취소 제안 유지 |

## 열려 있는 미해결

1. **부하 오염 재측정** (Phase 1 지연 숫자) — 러너는 수정됨(633d7fb).
2. **비율 0.5 에서 B 0.66ms vs C 0.41ms** — Phase 4 에서 "B 지연 = 미스 꼬리" 방향은
   실측됐으나 비율 0.5(축출 없음)엔 그대로 적용 불가. 재측정 필요.
3. **측정 절차 개선 (다음 스윕의 spec 에 사전 등록할 것)** —
   ① 워밍업을 측정 시드로 ② 무릎·우열 판정을 지연 기반 복합 규칙(p50·p95·드롭 우선순위)으로
   ③ 계단 길이 30s 를 spec 에 명시 (`docs/phases/phase-4-load/gate.md` 다음 페이즈 입력).
4. **phase-0 P2(B 무릎 ≈ A ±15%) 확정 재측정** — 판정이 런 선택(20s vs 30s)에 민감했다.
   30s 프로토콜을 사전 등록하고 반복 측정해야 "일치"가 확정된다 — Phase 2 스윕 때 병행
   (`docs/phases/phase-0-saturation/gate.md` P2 절).

## 물리 환경

| 역할 | 접속 | 사양 |
|------|------|------|
| 조종 | 이 노트북 192.168.55.114 | 24C/36G — **주의: BPC 로는 WiFi 경로라 부하 생성 실격** |
| 부하 APC | `ssh jun@192.168.55.9` | 8C/15G · **상주 서비스 ~6G — k6 는 반드시 `-m 6g` 캡** |
| SUT BPC | `ssh jun@192.168.55.164` | 16T/31G · i7-11700F |

- BPC ollama 상주(11434) — 런마다 loadavg 감시.
- APC 는 2026-07-18 OOM 동결 전과가 있다 — 캡 없는 k6 금지 (TIMELINE 문제 ⑥).
- 한 지점 실행: `python3 scripts/run_arm.py <arm> --keyspace N --skew S --rps R`
  (주의: run_arm.py 의 CPU_TOTAL=4.0 은 히트율 페이즈용 — 포화 페이즈는 0.5 를 수동 구성)
